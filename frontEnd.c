int main(){
  // Start thread to ping primary and if tout elect new primary
  // Start thread to receive from client and send to server
  // Start thread to receive from server and send to client
  return 0;
}
